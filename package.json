{
  "name": "unimma-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "build:docs": "pnpm --filter docs build",
    "build:ui": "pnpm --filter ui build",
    "lint": "pnpm -r lint"
  },
  "dependencies": {
    "@nuxt/icon": "2.2.1",
    "@nuxt/ui": "^4.6.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@iconify-json/ph": "^1.2.2"
  }
}
